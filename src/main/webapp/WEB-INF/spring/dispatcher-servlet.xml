<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans   http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc     http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- properties 로딩 (컨트롤러 @Value + mvc:resources 에서 사용) -->
    <context:property-placeholder location="classpath:application.properties" ignore-unresolvable="true"/>

    <mvc:annotation-driven/>
    <context:component-scan base-package="com.maddit.controller"/>
    <context:component-scan base-package="com.maddit.admin.controller"/>

    <!-- 관리자 인증 인터셉터 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/maddit/admin/**"/>
            <mvc:exclude-mapping path="/maddit/admin/login"/>
            <mvc:exclude-mapping path="/css/**"/>
            <bean class="com.maddit.admin.interceptor.AdminAuthInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- JSP ViewResolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 정적 자원 -->
    <mvc:resources mapping="/css/**"    location="/css/"/>
    <mvc:resources mapping="/js/**"     location="/js/"/>
    <mvc:resources mapping="/images/**" location="/images/"/>

    <!-- 업로드 파일 서빙 (application.properties의 file.upload.base-path) -->
    <mvc:resources mapping="/upload/**" location="file:${file.upload.base-path}/"/>

    <!-- MultipartResolver -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.support.StandardServletMultipartResolver"/>

</beans>
